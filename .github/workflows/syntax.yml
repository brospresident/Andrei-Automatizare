name: syntax

on: push

jobs:
  task1:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
          python-version: 3.7
    - run: echo It works
    - run: echo Made by Andrei Radu
    - run: | 
        if [ $("python add 1 2") != "3" ] exit 1 fi
        if [ $("python add 2 2") != "4" ] exit 1 fi 
        if [ $("python add 3 2") != "5" ] exit 1 fi
        if [ $("python add 6 2") != "8" ] exit 1 fi
        if [ $("python add 8 3") != "11" ] exit 1 fi 